import React from 'react'
import useTitle from '../../component/hook/useTitle'

const Activities = () => {
  
   useTitle('Activities')
  return (
    <div>Activities</div>
  )
}

export default Activities